‎# Hostel Management System (C++ OOP Project)
‎
‎### Project Member:
‎* **Ishal Ahmad** *
‎
‎**Instructor:** Dr. Muhammad Kazim  
‎**Department:** Artificial Intelligence, University of Malakand  
‎
‎---
‎
‎## About the Project
‎This is a console-based application designed to automate day-to-day hostel operations, such as keeping track of student records, checking room availability, and allocating rooms. 
‎
‎Instead of writing isolated, basic code examples, I am building a complete system that connects different OOP concepts together into a single application. I am using a version-by-version approach to develop this project safely.
‎
‎## OOP Concepts We Are Covering
‎As I progress through the versions, this project will implement:
‎1. **Classes & Objects:** Blueprints for `Room`, `Student`, and `Hostel`.
‎2. **Encapsulation:** Making data members private and accessing them safely through public getter/setter functions.
‎3. **Inheritance:** Creating a base `Person` class that child classes like `Student` and `Staff` will inherit from.
‎4. **Polymorphic Arrays:** Using base class pointers (`unique_ptr`) inside vectors to display specific data at runtime.
‎5. **Custom Exceptions:** Handling runtime issues (like trying to allocate an already full room) using `try`, `catch`, and `throw`.
‎6. **Templates:** A generic `Repository` class to manage data collections cleanly.
‎7. **File Handling:** Saving records into a text file so our data isn't lost when the program closes.
‎
‎## Version History & Workflow
‎I am developing this project in 5 stages:
‎
‎* **Version-01:** The basic layout. Contains the structural classes for `Room` and `Student` and a functioning main menu.
‎* **Version-02 :** Adding private data encapsulation, overloaded search queries, and custom error exceptions.
‎* **Version-03:** Introducing the `Person` base class, inheritance, and smart pointers.
‎* **Version-04:** Setting up operator overloading and generic templates.
‎* **Version-05:** Adding file handling to save records and formatting the final output tables.
‎
